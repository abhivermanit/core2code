# Environment variable template — copy to .env and fill in values.
# NEVER commit .env to git.

# App
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=

# Auth
JWT_SECRET=
SESSION_SECRET=

# Add project-specific variables below as the stack is defined (Phase 1)
