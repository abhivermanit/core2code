# {{PROJECT_NAME}} — Web (Next.js)

Next.js frontend for **{{PROJECT_NAME}}**.

## Setup

```bash
{{PACKAGE_MANAGER}} install
{{PACKAGE_MANAGER}} dev
```

Copy `.env.example` to `.env.local` and fill in the values (including your Clerk
publishable key — see `../auth/README.md`).
