# {{PROJECT_NAME}} — API (NestJS)

NestJS backend for **{{PROJECT_NAME}}**.

## Setup

```bash
{{PACKAGE_MANAGER}} install
{{PACKAGE_MANAGER}} start:dev
```

Copy `.env.example` to `.env` and set `DATABASE_URL` (see `../database/README.md`).
